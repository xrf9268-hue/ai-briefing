【输入】
BRIEFING_TITLE: {{briefing_title}}
CLUSTERS_JSON: {{clusters_json}}   # 由上游聚类/精排后生成，包含每簇的代表样本、候选摘要句与 urls

【你的任务】
1) 对每个簇生成一个 topic：
   - label：3~8 字主题名（不夸张、不引入新事实）；
   - headline：一句话总括“变化/能力 + 影响/用法”，≤140字；
   - bullets：1~4 条要点，建议结构：What（发生了什么/能力）→ Impact（对工程实践的影响）→ Limits（限制/风险）。
2) 合并语义重复要点：保留信息密度最高的表达，urls 可以包含多源。
3) 仅使用输入内容写作；不得臆测或补充来源未给出的数据。
4) 产出仅为 JSON（严格符合 Schema），不要任何解释性文字。
5) 缺乏足够证据的要点，用“仍在评估/尚不确定”表述，并照样给出来源 urls。

【质量检查】
- 每条 bullet 必须至少一个 url；
- 不得出现 “根据网络传言/网友表示”等表述；
- 术语首次出现可简注释（≤10字）；
- 严格 JSON，键名与嵌套结构不可偏离。
